# Ghostty Configuration
# Synced via dotfiles repo
#
# Los COLORES viven en theme.conf, que es GENERADO desde themes/<nombre>.sh por
# scripts/theme.sh. Cambiá de tema con `theme <nombre>`; no toques colores acá.
config-file = theme.conf

# Font Configuration
font-family = Google Sans Code NF
font-size = 15

# Shell Configuration
shell-integration = bash
command = /opt/homebrew/bin/bash

# Cursor Shape & Blink
# Bloque grueso (no la rayita delgada).
cursor-style = block
# Blink nativo OFF: el shader 9-smearbreathe genera su propio blink sincronizado
# con el breathe. Si usás otro efecto y querés parpadeo, ponelo en true.
cursor-style-blink = false
# Evita que bash/programas cambien la forma del cursor a barra.
shell-integration-features = no-cursor

# Cursor FX — efecto seleccionable con el comando `cfx` (ej: cfx bloom).
# active_cursor.glsl es GENERADO por scripts/theme.sh a partir del efecto
# elegido en shaders/effects/, tintado con el accent del tema activo.
custom-shader = shaders/active_cursor.glsl
# Anima siempre: necesario para los fades/pulsos de los efectos.
custom-shader-animation = always

# Window Configuration
window-padding-x = 10
window-padding-y = 5
window-decoration = true
window-theme = dark

# Window size (centered rectangle, wider than tall)
window-width = 180
window-height = 45

# Performance
macos-option-as-alt = true

# ============================================================
# Keybindings — Setup Ghostty-only (reemplazo de Zellij)
# ============================================================
# Splits (paneles) — cmd+d nuevo split derecha, cmd+shift+d abajo
keybind = cmd+d=new_split:right
keybind = cmd+shift+d=new_split:down
# Navegación entre splits estilo vim (hjkl)
keybind = cmd+alt+h=goto_split:left
keybind = cmd+alt+j=goto_split:down
keybind = cmd+alt+k=goto_split:up
keybind = cmd+alt+l=goto_split:right
# Zoom (fullscreen del split activo) y cerrar
keybind = cmd+shift+enter=toggle_split_zoom
keybind = cmd+w=close_surface
# Igualar tamaño de todos los splits
keybind = cmd+ctrl+r=equalize_splits
# Redimensionar el split activo
keybind = cmd+ctrl+h=resize_split:left,20
keybind = cmd+ctrl+j=resize_split:down,10
keybind = cmd+ctrl+k=resize_split:up,10
keybind = cmd+ctrl+l=resize_split:right,20

# Layout en cruz (2x2): Ghostty mapea un keybind a UNA sola acción, así que no
# existe un atajo único de layout. La receta de cuatro pulsaciones es:
#   cmd+d  →  cmd+shift+d  →  cmd+alt+h  →  cmd+shift+d
# y después cmd+ctrl+r para igualarlos. El comando `cross` hace lo mismo de una
# sola vez (ver bash/.bashrc y scripts/ghostty-cross.sh).

# Tabs (pestañas)
keybind = cmd+t=new_tab
keybind = cmd+shift+l=next_tab
keybind = cmd+shift+h=previous_tab
# (cmd+1..9 para saltar a una pestaña ya viene por defecto en Ghostty)

# Nota: ya no hace falta desvincular alt+hjkl/arrows (eran para Zellij);
# así alt queda libre para mover palabras en bash/readline.
